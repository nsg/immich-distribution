--- a/web/src/routes/+layout.svelte
+++ b/web/src/routes/+layout.svelte
@@ -100,8 +100,6 @@ const handleRelease = async (release?: ReleaseEvent) => {
       console.error('Error [VersionAnnouncementBox]:', error);
     }
   };
-
-  $effect(() => void handleRelease($release));
 </script>
 
 <svelte:head>
